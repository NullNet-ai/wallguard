stages:
  - build
  - test
  
main_build:
    only:
      - main
    stage: build
    tags: 
      - FreeBSD
    script:
      - make debug release test

main_test:
    stage: test
    only:
      - main
    tags:
      - FreeBSD
    dependencies:
      - main_build
    script:
      valgrind --leak-check=full --error-exitcode=1 wallmon_test