#ifndef PLATFORM_PFSENSE_H
#define PLATFORM_PFSENSE_H

/**
 * @brief Checks if the current platform is pfSense.
 * 
 * @return int Returns 1 if the platform is pfSense, otherwise returns 0.
 */
int is_pfsense();

#endif // PLATFORM_PFSENSE_H
